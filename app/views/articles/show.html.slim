h2.rina-title
  = @article.title
.content.markdown
  == @article.content_html

.create-comment
  = render 'shared/form_error_message'

  = form_for @comment, url: comments_path(article_id: @article.id) do |f|
    .form-group
      = f.text_area :message, rows: '3', class: 'form-control', placeholder: '留言...'
    .form-group
      = f.text_field :username, class: 'form-control form-comment', placeholder: '你的名字'
    .form-group
      = f.text_field :email, class: 'form-control form-comment', placeholder: '邮箱'
    .form-group
      = f.submit '提交', class: 'btn btn-default'

.form-group
  - @comments.each_with_index do |comment, index|
    - if index == 0
      .comment-diag
    .comment-wrapper
      p.name
        = comment.username
        |•
        span.created-at
          = comment.created_at.strftime("%Y-%m-%d %H:%M")
      p.comment-content
        = comment.message

.jiathis_style
  a.jiathis_button_tools_1
  a.jiathis_button_tools_2
  a.jiathis_button_tools_3
  a.jiathis_button_tools_4
  a.jiathis_counter_style

javascript:
  var jiathis_config = {
    title: "hello world",
    summary: 'xxxxxxxxxxx',
    pic:"http://blog.jiathis.com/wp-content/uploads/2012/02/ujian.jpg"
  }

script type="text/javascript" src="http://v3.jiathis.com/code/jia.js?uid=1" charset="utf-8"
